import { describe, it, expect } from 'vitest';

describe('App Component', () => {
  it('should render without crashing', () => {
    // Just a placeholder test to ensure testing is set up
    expect(true).toBe(true);
  });
});
